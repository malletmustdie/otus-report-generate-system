server:
  port: 8080

spring:
  application:
    name: api-gateway

services:
  config-control-service:
    base-uri: http://localhost:8090/api/v1/configs
  report-generator-service:
    base-uri: http://localhost:8083/api/v1/reports

management:
  endpoints:
    web:
      exposure:
        include: health, metrics, prometheus
  metrics:
    distribution:
      percentiles:
        http.server.requests: 0.99, 0.95, 0.9, 0.7, 0.5
      percentiles-histogram:
        http.server.requests: true
      minimum-expected-value:
        http.server.requests: 100ms
      maximum-expected-value:
        http.server.requests: 10s
      slo:
        http.server.requests: 10ms, 50ms, 100ms, 200ms, 300ms, 400ms, 500ms, 600ms, 800ms

logging:
  level:
    ru.elias.gateway: DEBUG