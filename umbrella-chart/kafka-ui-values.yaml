yamlApplicationConfig:
  kafka:
    clusters:
      - name: order-notification-kafka-cluster
        bootstrapServers: kafka.dev.svc.cluster.local:9092
  auth:
    type: disabled
  management:
    health:
      ldap:
        enabled: false